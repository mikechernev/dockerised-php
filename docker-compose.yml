version: '2'

services:
    web:
        image: nginxinc/nginx-unprivileged:alpine
        ports:
            - "8080:8080"
        volumes:
            - ./code:/code
            - ./site.conf:/etc/nginx/conf.d/default.conf
    php:
        image: php:fpm-alpine
        volumes:
            - ./code:/code
